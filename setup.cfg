[egg_info]
tag_build =
tag_date = 0

[tool:pytest]
# Scoping collection to `tests` keeps pytest away from the scratch files in
# `pltstat/sandbox`, which are local aids and not part of the package.
testpaths = tests
markers =
    slow: tests which pay the numba compilation cost of UMAP
